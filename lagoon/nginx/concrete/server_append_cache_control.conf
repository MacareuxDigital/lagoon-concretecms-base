# Static assets: don't log and won't pass them to PHP if 404
location ~* \.(jpg|jpeg|gif|png|css|js|ico|svg|webp|woff|woff2)$ {
        access_log        off;
        log_not_found     off;
        # Add 365 days expires to production
        add_header Cache-Control "public, max-age=31536000";
}